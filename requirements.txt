# Local data generation (Phase 1, step 1)
numpy>=1.26
pandas>=2.1
pyarrow>=14.0  # Phase 6 reads the exact persisted Phase 5 candidate Parquets

# Databricks control plane: catalog/schema DDL, file upload, SQL execution
# (Statement Execution API -- no cluster or databricks-connect required)
databricks-sdk>=0.30

# Phase 2 simulation engine: Beta/LogNormal fitting, normal CDF for the copula
scipy>=1.11

# Validation reporting: correlation heatmap, revenue histogram
matplotlib>=3.8
